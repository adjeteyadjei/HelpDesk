
<header class="navbar navbar-inverse navbar-fixed-top header" role="banner">
    <div class="">
        <div class="navbar-header">
            <a href="#" class="navbar-brand">Help Desk</a>
        </div>
        <nav class="navbar-collapse collapse" role="navigation" style="height: 1px;">
            <ul class="nav navbar-nav">
                <li class="">
                    <a href="/"><i class="fa fa-dashboard"></i>Dashboard</a>
                </li>
                <li>
                    <a ui-sref="tickets"><i class="fa fa-tags"></i>Tickets</a>
                </li>
                @*<li>
                    <a ui-sref="reports"><i class="glyphicon glyphicon-book"></i>Reports</a>
                </li>*@
                @if (User.IsInRole("Administrator") || User.IsInRole("TeamLead"))
                {
                    <li>
                        <a ui-sref="users"><i class="fa fa-user"></i>Users</a>
                    </li>
                }

                @if (User.IsInRole("Administrator"))
                {
                    <li>
                        <a ui-sref ="teams"><i class="fa fa-group"></i>Teams</a>
                    </li>
                    <li>
                        <a ui-sref="projects"><i class="glyphicon glyphicon-th-large"></i>Projects</a>
                    </li>
                    <li>
                        <a ui-sref="config"><i class="fa fa-cogs"></i>Configuration</a>
                    </li>
                }
            </ul>
            <div class="collapse navbar-collapse ">
                <ul class="nav navbar-nav navbar-right">
                    <li class="dropdown">
                        <a href="" class="dropdown-toggle" data-toggle="dropdown">Welcome  @User.Identity.Name<b class="caret"></b></a>
                        <ul class="dropdown-menu">
                            <li><a href="">My Profile</a></li>
                            <li class="divider"></li>
                            <li>
                                @Html.ActionLink("Sign Out", "LogOut", "Home")
                            </li>
                        </ul>
                    </li>
                </ul>
            </div>
        </nav>
    </div>
</header>